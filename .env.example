# 忍者之夜 — 环境变量示例
# 复制为 .env 或在 shell 中导出；默认值见 src/shared/timeouts.ts

# 决策窗口超时（毫秒）
NINJA_WINDOW_MS=60000

# 空房清理（毫秒）
NINJA_EMPTY_ROOM_TTL_MS=300000

# 对局结束后房间保留（毫秒）
NINJA_ENDED_ROOM_TTL_MS=600000

# 单 socket 每秒指令上限
NINJA_COMMAND_RATE_PER_SEC=10

# 断线座位保留（毫秒）
NINJA_DISCONNECT_RETAIN_MS=300000

# 服务端端口
PORT=3000
